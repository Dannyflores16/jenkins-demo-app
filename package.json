{
  "name": "jenkins-demo-app",
  "version": "1.0.0",
  "scripts": {
    "test": "node test/suma.test.js",
   "build": "echo Build completado && if not exist dist mkdir dist && copy index.js dist\\"
  }
}